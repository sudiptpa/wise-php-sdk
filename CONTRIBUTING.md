# Contributing

1. Fork and create a feature branch.
2. Run `composer install`.
3. Run `composer qa` with PHP 8.2+ (example: `/usr/bin/php8.5 /usr/local/bin/composer qa`).
4. Submit a pull request with tests and docs updates.
